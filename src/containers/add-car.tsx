import * as React from "react";
import { PageContent } from "../components/elements/page-content/page-content";

export const AddCar: React.FC = () => {
  return (
    <main>
      <PageContent>add new car</PageContent>
    </main>
  );
};
